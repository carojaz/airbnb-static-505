<section id="show">
  <img class="img-show" src="<%= @flat["imageUrl"] %>" />
  <h1><%= @flat['name'] %></h1>
  <h6><%= @flat['price']%> €</h6>
</section>

<%= link_to 'Back to homepage', root_path %>
